# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (PostgreSQL)
# Standard connection string format: postgres://user:password@host:port/database
DATABASE_URL=postgres://your_user:your_password@localhost:5432/unihelp_db

# Separate database variables (if preferred)
DB_USER=your_user
DB_PASSWORD=sankalpa123
DB_HOST=localhost
DB_PORT=5432
DB_NAME=unihelp_iptm

# Security
JWT_SECRET=your_jwt_secret_here
REFRESH_TOKEN_SECRET=your_refresh_secret_here

# CORS / Client Link
CLIENT_URL=http://localhost:5173
