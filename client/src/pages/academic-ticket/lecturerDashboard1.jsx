import React from "react";
import QuizManagement from "../../components/academic-ticket/lec_dashboard/Quiz";

const lectureDashboard1 = ({ embedded = false }) => {
  return <QuizManagement embedded={embedded} />;
};

export default lectureDashboard1;
